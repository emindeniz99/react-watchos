# Status — what's actually verified

The honest "can I rely on X today?" view, kept **separate from the forward
plan** in [roadmap.md](./roadmap.md) and from the work queue in
[code-review-2026-06-25-1817-merged.md](./code-review-2026-06-25-1817-merged.md)
(THE backlog). roadmap.md describes *intent and history*; this file describes
*verified current state* and links every claim to evidence. When the two
disagree, **this file wins** for "is it real yet?".

> Why this exists (CX-027): "shipped" was being claimed inline next to
> aspirational items, and two features were overclaimed — on-device AI and the
> predictive part of Smart Stack relevance. This page is the single place where
> a status claim has to be falsifiable.

## Verification ladder

Every capability sits at one level. Higher levels subsume the lower ones.

| Level | Means | How it's checked |
|---|---|---|
| ① **Logic-tested** | Pure logic unit-tested off-device | `pnpm test` (**693** JS, vitest — incl. the codegen-drift check, green on Linux since the swift-format implicit-stdin fallback) + `swift test` (**401** green, Linux/macOS: Core/Support/Runtime — the SwiftPM package first compiled + tested on **Linux** 2026-07-09, Swift 6) — counts drift; treat as point-in-time (**2026-08-20**) |
| ② **Builds for watchOS** | The SwiftUI host/widget compiles for the watch, and the suite runs on the watch simulator | `pnpm test:swift:watch` (`xcodebuild test`, watchOS sim) — **TEST SUCCEEDED locally 2026-08-10** (388 tests, 0 failures, watchOS 26.5 sim, Xcode 26.6 — a SUPERSET of ①'s Linux run, which cannot compile the `#if os(watchOS)` cases at all: it was 388 against Linux's 375 that day, and ①'s total has since reached 388 of its own, so the two numbers matching is a coincidence, not the same suite; previous green runs 2026-08-06 on Xcode 26.3, 2026-07-04). Each new Xcode has charged a toll: the 08-06 run was the first compile of everything blind-written 07-27→07-29 and surfaced 4 real breaks (PedometerBridge region isolation, `WorkoutScheduler` sendability, `WorkoutAlertMetric.countPerMinute`, `finishWorkout`'s optional `HKWorkout`); the 08-10 run fixed 2 more that Xcode 26.6's stricter SE-0414 enforcement promoted to errors (CalendarBridge's implicitly-@MainActor `json` helper, WorkoutBridge's twice-sent `HKWorkoutConfiguration`) — first caught by `build.yml`, which HAS been running on schedule since 2026-08-07 (red 08-07→08-10 on exactly those two; Actions got enabled with the standalone repo, so B1's "never run" is history). The app/widget **app targets** built green 08-06 (B3): `run:watch` compiled the demo app + embedded widget, re-signed with the sim-safe entitlement set, installed, launched, and a Counter interaction re-render was verified on the S11 46mm sim (AXe-driven ③ smoke). |
| ③ **Device/sim-verified** | Exercised on a watchOS simulator or real Apple Watch | manual; noted per row |
| ⛔ **Blocked** | Needs an unreleased OS/SDK, or hardware we don't have | noted per row |

Linux `swift test` (①) verifies the **wire decode** of props — not the SwiftUI
view. So for anything that renders or touches native frameworks, the **watchOS
build (②) is the real gate**, and behavioral correctness isn't guaranteed until
③. Most native features below are at ② with ③ as the standing gap.

## Capability matrix

| Capability | Level | Evidence / note |
|---|---|---|
| React → JSON tree → SwiftUI renderer + sync commit pipeline | ③ | reconciler [renderer.ts](../js/src/renderer.ts); wire decode in `swift test`; host builds green — **plus first on-sim ③ run (2026-07-05, Ultra 3 49mm):** the full React→JSON→SwiftUI→commit loop drives live UI — Counter `useState` re-render (0→3), Hydration + Shopping App-Group atomic counter (`counterAdd`/`counterValue`) increment + persist across navigation, NavigationLink push/pop, Toggle image swap. Caveat: the App-Group path only works when the app is **signed** — a `CODE_SIGNING_ALLOWED=NO` build strips the `group.com.emindeniz99.reactwatch` entitlement and every shared-state screen silently reads 0; build ad-hoc signed (`CODE_SIGN_IDENTITY="-"`). Also render-verified on the same run: Gallery (Slider, ProgressView, ZStack), a MapKit Map (3 tinted SF-Symbol annotations + polyline route, new `/map` demo), and TabView draws its 2 pages + page-indicator dots (the page-advancing swipe wouldn't fire via synthetic touch — a sim-input nuance, not a renderer defect; that run predates the `style` prop, so it was SwiftUI's default pager, NOT `.verticalPage`, which landed 2026-08-10). Crown/gesture *feel*, widgets/complications, BLE/phone/AI/OTA remain ② (not yet exercised on-sim). **Plus first REAL-DEVICE run (2026-07-05, physical Apple Watch Ultra 3, watchOS 26.5, paired iPhone 14 Pro):** the "React Watch" scheme — *properly* automatic-signed against a real team (`D68Q862K33`), with the App Group capability provisioned by the portal (no ad-hoc / entitlement-stripping hacks, unlike the sim) — builds, installs, and **runs on-wrist**: the full QuickJS + renderer → SwiftUI stack boots and renders on hardware. Scope so far = boots + renders; the hardware-only validations (Taptic haptics, Digital Crown feel, HealthKit heart-rate + GPS location streams, complication on the live watch face) are the natural next tick. Gotchas hit: device install needs **Developer Mode** on the watch + Mac/watch on the **same Wi-Fi** (wireless deploy, no USB), and both the deploy tunnel and Xcode's `dyld_shared_cache_extract_dylibs` step need several GB of **free disk**. |
| 41 UI primitives (Stack/Text/FormattedText/Gauge/Map/Alert/Sheet/Section/Grid/Chart/Toolbar/…, count from the generated [api/capabilities.md](./api/capabilities.md)) | ② | contract is single-sourced + drift-guarded ① ([component-contract.test.ts](../js/test/component-contract.test.ts)); views in `NodeView.swift` build green (②) |
| Digital Crown, gestures, Slider/Stepper/DatePicker/Picker | ② | prop decode ①; views ② — on-device feel is ③ |
| WatchConnectivity — watch→phone (`sendToPhone`) + phone→watch | ② | `PhoneConnectivity` builds ②; iPhone side wired in the companion app (`react-native-watch-connectivity` listener **calls the reply handler**, so the watch's `sendToPhone` promise settles instead of timing out); the end-to-end paired exchange is ③ (needs a paired sim/device pair). **Background channels (ARCH-12, 2026-07-16)**: `updateApplicationContext`/`transferUserInfo` outbound + `onApplicationContext`/`onUserInfo` inbound, split by delivery semantics — JS ① (connectivity suite 7/7 incl. channel isolation); the new watch-only delegates compiled + suite-ran on the watch sim ② (2026-08-06) |
| WatchConnectivity **file transfer + session state** (`transferFile`/`cancelFileTransfer`/`outstandingFileTransfers`/`deleteReceivedFile`/`readReceivedFile` + `watchConnectivity.file`/`.fileTransfer`/`.state` pushes) — feature `connectivity` | ② JS+Support / 🔴 **③ needs PAIRED DEVICES** | **2026-07-29.** `transferFile` resolves once QUEUED with a bridge-minted id (Apple documents the transfer as throttled, surviving suspension, and able to complete in a *later launch*, so parking the invoke would blow the 30 s watchdog every time); completion rides the push channel and reports `id: null` for a transfer this launch didn't queue. The inbound move happens SYNCHRONOUSLY inside `session(_:didReceive:)`, before the main hop — Apple deletes the file when that method returns. Inbox naming, retention and the containment check `deleteReceivedFile` rests on are in `ReactWatchSupport` → ① (`FileInboxTests`); ARCH-11 fixtures both directions ①; the silent-failure rules pinned by `platform-data-guards.test.ts` ① (26 after the hardening pass), each negative-checked. **③ is impossible on a simulator**: Apple states both that the Simulator doesn't support `transferFile(_:metadata:)` and that it doesn't call `session(_:didReceive:)` — this needs two paired physical devices. **2026-07-29 hardening:** the read path is now `readReceivedFile` (base64, chunked, containment-checked, gated on `connectivity`) rather than `fetch("file://…")`, which reported `ok === false` on a successful read, could not read past 5 MiB at all, and needed the `network` feature a `connectivity`-only bundle does not have — so the unproven `file://` leg is no longer on it. Every inbound plist is reduced to JSON in `deliver` (one boundary, not four call sites), and an unreadable modification date now KEEPS a file instead of deleting it. `getConnectivityState().reachable` is documented as observability, **not** a send gate ([notes/watchconnectivity-reliability.md](../notes/watchconnectivity-reliability.md)) |
| **Calendar + reminders reads** (`requestCalendarAccess`, `getCalendarEvents`, `getReminders`) — feature `calendar` | ② | **2026-07-29.** EventKit, read-only v1. `requestFullAccessToEvents`/`ToReminders` at watchOS 10.0 = the package floor, so no `@available` gate anywhere; the deprecated `requestAccess(to:)` and `NSCalendarsUsageDescription` are deliberately unused. A read-only API still needs FULL access (Apple exposes no read-only grant), and `writeOnly` is reported as its own status rather than collapsed into `denied`. Entity vocabulary, window rules and the limit clamp are in `ReactWatchSupport` → ① (`CalendarPlanTests`); ARCH-11 fixtures ①; plugin `calendar: true` emits both `…FullAccessUsageDescription` keys and **no entitlement** (the macOS one is sandbox-only) ①. The EventKit calls are ②; the TCC sheet, the `.writeOnly` mapping and the prebuilt Info.plist are ③ |
| **Always-On / reduced luminance** (`onLuminanceReduced`) + **scene phase** (`onScenePhase`) | ② | **2026-07-29.** `EnvironmentValues.isLuminanceReduced` (watchOS 8.0, verified) read ONCE at `ReactWatchRootView` — never in `NodeView`, which would fan one global signal across every node. A pure push event: no schema entry, no host method, no HostPolicy feature. TWO initial-value mechanisms, both required and both guard-tested: `.onChange(of:initial:true)` (watchOS 10.0) plus a re-push at the end of `boot()` once `jsReady`, because the first can land while `pushNativeEvent` still reaches nobody. JS ① (`appState.test.tsx`, incl. the instant-commit path). Wrist-down is **not reproducible on a simulator**, so ③ is device-only. **2026-07-29:** `onScenePhase` gives the long-standing `scenePhase` push a typed wrapper (allowlist, not a cast), and BOTH events are replayed to a late subscriber — level-triggered state only, so a replay cannot fabricate an event. `scenePhase` has no boot-time push, so its replay covers "subscribed after a transition" only. A wrist-down app stays `active`: the two are different questions and a test pins that they do not share a stream |
| `fetch` over URLSession (WHATWG subset) | ② | `FetchPlan` request/response parsing ① (`FetchPlanTests`); URLSession orchestration ② |
| Sensors push streams (heart rate, motion, gyroscope, location, **pedometer**) | ② | builds ②; real sensor behavior is ③ (needs a device). **2026-07-29:** `startPedometer` + `queryPedometer` (CMPedometer, ~7-day on-device history) added under the existing `sensors` feature — same framework, same `NSMotionUsageDescription`, same single "Motion & Fitness" consent as the shipped motion streams. Payload assembly + window validation are in `ReactWatchSupport` → ① (`PedometerReadingTests`); the CoreMotion calls are ② |
| **HealthKit reads + the live sample stream** (`requestHealthAuthorization`, `queryHealthStatistics`, `queryHealthDailyStatistics`, `queryHealthSamples`, `querySleepSamples`, `queryWorkoutHistory`, `queryActivitySummaries`, `startHealthUpdates`) — feature `health` | ② | **2026-07-29.** Descriptor-based queries (`HKStatisticsQueryDescriptor`/`HKStatisticsCollectionQueryDescriptor`/`HKSampleQueryDescriptor`, watchOS 8.5 — under the v10 floor, no gates). `queryHealthDailyStatistics` (the `healthBuckets` follow-up, taken the same day) turns a week chart from seven query round trips into one; buckets are contiguous (`enumerateStatistics`, so an empty day is `value: null`, not a gap), anchored on the caller's `startMs`, and a window past the 1000-bucket ceiling is refused rather than truncated. Request validation, the per-type unit table and the statistic/type legality Apple enforces by throwing are in `ReactWatchSupport` → ① (`HealthQueryPlanTests`, `swift test`); ARCH-11 fixtures both directions ①; the queries themselves ②. **③ is device-only**: [running-on-sim.md](./running-on-sim.md) signs the sim build *minus* the `healthkit` entitlement deliberately, so no simulator run can exercise these. A granted READ is indistinguishable from a denied one (Apple's design) — `value: null` / `[]` means denied-or-absent-or-outside-the-granted-window, and the JSDoc says so. **2026-08-20 — the read vocabulary widened to seven quantity types:** `heartRateVariabilitySDNN` (wire unit `"ms"`) and `restingHeartRate` (`"count/min"`) joined it; both are `HKQuantityTypeIdentifier` at watchOS 4.0, so the family stays gate-free, and both are DISCRETE, so `sum` is refused before the query runs. Support-side unit table + legality ① (`HealthQueryPlanTests`); the two new `HKQuantityType`/`HKUnit` arms are `#if os(watchOS)` and reach ② only on the next watch-sim build. **2026-08-20 (second increment) — fourteen quantity types.** Seven more, each re-verified against `developer.apple.com/tutorials/data/documentation/healthkit/hkquantitytypeidentifier/<type>.json` (watchOS `introducedAt`, beta/deprecated flags, and the docs' own "measure cumulative values" / "measure discrete values" sentence): CUMULATIVE — `appleExerciseTime` (2.2, `"min"`), `basalEnergyBurned` (2.0, `"kcal"`), `flightsClimbed` (2.0, `"count"`), `appleStandTime` (6.0, `"min"`); DISCRETE — `respiratoryRate` (2.0, `"count/min"`), `vo2Max` (4.0, `"ml/kg/min"` — Apple's own spelling for this sample type), `walkingHeartRateAverage` (4.0, `"count/min"`). watchOS `introducedAt` tops out at 6.0, none beta or deprecated, so the family remains `@available`-FREE — stated from the fetched JSON, not assumed. **Verification level:** Support-side unit table, the cumulative/discrete partition and the statistic legality ① (`HealthQueryPlanTests`, `swift test`, now driven off `allCases` so a fifteenth type cannot land unpinned); the Host `HKQuantityType`/`HKUnit` arms and their watchOS mapping tests are `#if os(watchOS)`, so Linux cannot compile them — but since cb005d1 the `watchos-tests` job (`xcodebuild test` on the watch sim) runs on every `pull_request` touching `js/swift/**` as a REQUIRED check, so they reach ② on this change's own PR run rather than on some later manual build; ③ remains device-only for the whole row. One honest gap, recorded rather than guessed: Apple documents no canonical `HKUnit.unitString` spelling table — its `HKUnit(from:)` page is the PARSER's input vocabulary, not a promise about what a composed compound prints back, and it accepts both `L` and `l` for litres while its VO2 max entry (`ml/kg/min`) breaks the same page's own "only one division symbol" rule. So `vo2Max` joins `oxygenSaturation` on the watchOS test branch that pins the unit FAMILY (re-derived from `HKUnit`) instead of a spelling, backed by a CONVERSION test that pins the magnitude that actually ships (1 L/(kg·min) must read 1000 in the bridge's unit — `is(compatibleWith:)` alone is prefix-blind), while the wire string ↔ `HKUnit` expression pairing stays pinned on Linux by `health-package-guards.test.ts`, now matching each arm WHOLE rather than by substring. Also corrected here rather than left standing: the illegal-pairing refusal used to attribute the whole rule to Apple, but Apple documents `.mostRecent` as an option outside the `discrete*` family with no stated cumulative restriction — narrowing each type to ONE aggregate family is this package's rule, and the message and `isLegal` doc now say so. The plugin's `NSHealthShareUsageDescription` default was widened in the same change (exercise/stand time, respiratory rate — the sheet's own row label — cardio fitness, flights climbed, resting energy) — the 15c6840 under-promise bug, third occurrence. **2026-08-20 (third increment) — the SAVED WORKOUT HISTORY read (`queryWorkoutHistory`), the second of the two gaps the maintainer named.** Until now the package could START a workout (`startWorkout`, feature `workouts`) and read live metrics, but nothing could read what was SAVED — a watch app could not render "your last five runs". The query is `HKSampleQueryDescriptor` + `HKSamplePredicate.workout(_:)` (both watchOS 8.5): because the predicate is generic over `HKWorkout` the result is `[HKWorkout]` with no cast, so no row can be silently dropped by a failed `as?`. Energy and distance are read with `HKWorkout.statistics(for:)` (watchOS 9.0 — the ONE symbol in this file above 8.5, still far under the v10 floor, so the family stays `@available`-FREE; js/src/health.ts's module doc was corrected from "8.5 or below" rather than left standing), NOT with `totalEnergyBurned`/`totalDistance`, which Apple deprecated (watchOS 11.0 / 27.0) and which cannot tell "no samples" from zero. `nil` therefore rides the wire as `null`, not `0`: an indoor yoga session records no distance at all, which is a different fact from covering zero metres (and `null` covers two more cases the JSDoc now names — a workout another app saved as a bare TOTAL with no per-sample data behind it has no statistics to compute, and a quantity type the user declined). DISTANCE is read under the type the ACTIVITY records it in — a ride's metres are `distanceCycling`, a swim's `distanceSwimming` — through `WorkoutDistance`, ONE table now shared with the live workout, which already carried that rule: a fixed `distanceWalkingRunning` read would have reported `null` for every ride in the list and called it "measured nothing", and the same ride would have shown 12 km while running and "—" once saved. `distanceRowing`/`distancePaddleSports`/`distanceCrossCountrySkiing`/`distanceSkatingSports` are watchOS **11.0**, above the floor, so those activities take the walking/running default rather than drag the package's first `@available` gate into a lookup table. The WINDOW rule is HealthKit's default `options: []`, which Apple documents as `endDate >= start AND startDate < end` — OVERLAP, not start-containment; academic for a near-instantaneous quantity sample, renderable for an hour-long workout, so the JSDoc states overlap rather than the `[startMs, endMs)` framing the other reads use. **Feature `health`, deliberately not `workouts`** — `workouts` authorizes RECORDING one (a write, background execution, the single session slot), while this discloses every workout the user has, including ones other apps saved; SupportTests already pins the two as separately deniable, and `health-package-guards.test.ts` now pins this method's feature id so the two cannot be quietly merged. The wire shape is what a LIST renders and no more — `id` (the `HKObject` UUID, a stable key and the same id `WorkoutState.endedWorkoutId` reports), `startMs`/`endMs`, `durationMs` (HealthKit's own `duration`, which EXCLUDES paused time, so it is not `endMs - startMs`), an OPTIONAL `activityType` (omitted, never guessed, when this binary's vocabulary has no name for a stored case — the generated `WorkoutActivityName.name(for:)` table read backwards, no second mapping), and nullable `activeEnergyKcal`/`distanceMeters`. Deliberately NOT exposed, so a later detail-view method can add them without moving this shape: `metadata`, `device`, `sourceRevision`, `workoutEvents`, the full `allStatistics` map, and `HKWorkoutActivity` multisport segments (the standing deferral). No activity FILTER either — the cap applies to the whole window, and the JSDoc says so rather than implying a filtered ask is cheap. **Authorization:** `HKObjectType.workoutType()` is neither a quantity nor a category type, so it reaches the sheet through its own `workoutHistory` flag on `requestHealthAuthorization` (the `sleep` precedent verbatim), and the query requests it itself so a caller who never ran the sheet gets a prompt rather than a silently empty list. That flag asks for MORE than the workout type: `HKWorkout.statistics(for:)` is computed from the `HKQuantitySample`s ASSOCIATED with the workout, each authorized on its own, so the set is workouts + `activeEnergyBurned` + every distance type the table can return — otherwise a history list could report `null` energy and `null` distance forever under a grant the user was never asked for. **No new entitlement and no new usage-description KEY** — `com.apple.developer.healthkit` + `NSHealthShareUsageDescription` already cover it — but the sheet does render a new ROW, so the plugin's default read sentence gained "your saved workouts" (the 15c6840 under-promise bug, fourth occurrence), pinned in `plugin.test.ts`. **Verification level:** window/limit decode + the refusal messages ① (`HealthQueryPlanTests`, `swift test` 391 on Linux); ARCH-11 fixtures both directions ① (`invoke-queryWorkoutHistory-request/response.json`, the response fixture carrying a workout with `null` energy AND `null` distance and no `activityType`, so the nullable half is decoded by the strict Swift decoder rather than asserted in prose); the producer's key set ① (`invoke-producer-keys.test.ts`); the workout type's identifier, the energy/distance units, the activity -> distance table and the claim that everything the summary READS is in the set it ASKS for, all ② in `HealthQueryBridgeMappingTests` on this PR's required `watchos-tests` job (compilation alone would not have caught any of them); the query itself ② there too; ③ device-only for the whole row, unchanged — the sim build is signed without the `healthkit` entitlement, so no simulator run can return a saved workout. The empty-array caveat is the row's own: `[]` still means denied-or-absent-or-outside-the-granted-window. **2026-08-20 (fourth increment) — the ACTIVITY RINGS with their GOALS (`queryActivitySummaries`), the highest-value item on the maintainer's list.** Until now no read in this package could report a goal: `appleExerciseTime` gives the minutes and stops there, so an app could know a user exercised 23 minutes and not whether that closed the ring — and since a ring IS a value measured against a goal, a rings screen or complication was not expressible at all. The query is `HKActivitySummaryQueryDescriptor` (watchOS 8.5, the same descriptor family as every other read here — NOT the callback class `HKActivitySummaryQuery`, whose docs JSON carries no availability at all), and every symbol it touches was re-verified against `developer.apple.com/tutorials/data/documentation/healthkit/hkactivitysummary/<property>.json`: `activeEnergyBurned`/`activeEnergyBurnedGoal`/`appleExerciseTime`/`appleStandHours`/`dateComponents(for:)` are watchOS **2.2**, `activityMoveMode`/`appleMoveTime`/`appleMoveTimeGoal` are **7.0**, and `exerciseTimeGoal`/`standHoursGoal` are **9.0** — all under the v10 floor, so the family stays `@available`-FREE. **The request is a range of calendar DAYS (`startDate`/`endDate`, `"YYYY-MM-DD"`, INCLUSIVE at both ends), not a `{startMs, endMs}` window** — the one shape deviation in this family, and it is forced: HealthKit matches a summary by `DateComponents` identifying "the day as perceived by the user", which Apple's own parameter doc notes "may be longer or shorter than 24 hours (for example, if the user traveled across time zones)". No instant means that day on its own, and the conversion is where the off-by-one lives — a caller sending `Date.UTC(y, m, d)` for "today" would ask for yesterday's rings west of Greenwich and get a plausible wrong answer rather than an error. So the day rides the wire as itself and nothing converts. **The invariant that would otherwise fail SILENTLY:** those components must carry a `calendar` (Apple: "the date components must have a valid calendar property") — a set without one matches NOTHING, with no throw, reaching the caller as `[]` indistinguishable from "you have no rings". So the bridge builds no `DateComponents` at all: `ActivityDay.components` in ReactWatchSupport attaches `ActivityDay.calendar` (gregorian by identifier, never `Calendar.current`, which on a Buddhist or Japanese-era locale would report year 2569 into a date string nothing can plot) by construction, `swift test` proves it on Linux, and `health-package-guards.test.ts` pins that the watchOS bridge assembles none of its own. The same one calendar reads the day back off each returned summary, so ask and answer cannot disagree about what day it is. **The day rule is a ceiling, refused not clamped:** 1000 days (`ActivitySummariesPlan.maxDays == HealthWindow.maxLimit` — one size rule for the family, and ~2.7 years, so every ring screen anyone builds fits), and there is deliberately NO `limit`: the answer is one row per day, so a cap could only mean "drop some of the days you asked for", which is a ring history with holes the caller cannot see. Malformed dates (`"2026-8-9"`, an ISO timestamp, 2026-02-30), an inverted range and an over-wide one all reject INVALID_REQUEST with a message naming the rule. **The wire shape is three value/goal pairs plus the day:** `date`, `moveMode`, `activeEnergyKcal`/`activeEnergyGoalKcal`, `moveTimeMinutes`/`moveTimeGoalMinutes`, `exerciseMinutes`/`exerciseGoalMinutes`, `standHours`/`standHoursGoal`. Rows come back OLDEST DAY FIRST — the descriptor takes no sort descriptors and Apple documents `result(for:)` only as "a snapshot of the current matching results", i.e. no order at all, so the bridge sorts by the same `serial` arithmetic the ceiling is counted with rather than leaving a seven-bar chart correct by luck. Every row still carries its own `date` because HealthKit returns NO row for a day it has no summary for (a watch on the charger), so a seven-day ask can resolve five rows and array position is not a date — and no backfill is synthesized, because a fake `0 / 500` row is indistinguishable on a chart from a day the user genuinely did not move. `activityMoveMode` is REPORTED rather than assumed: under-18 accounts (and anyone who picked Move Time in Settings) close a MINUTES ring, so a renderer that always drew energy would draw them a ring that never fills while their watch says it closed — both move spellings ride so the day a user switches modes needs no second query. The two watchOS 9.0 goals are OPTIONAL and cross as `null`, never as Apple's default 30 or 12: a ring with no goal cannot be drawn, and a substituted one draws an arc the user was never scored against. The LIVE goal spellings are used (`exerciseTimeGoal`, `standHoursGoal`); `appleExerciseTimeGoal`/`appleStandHoursGoal` are deprecated at watchOS 27.0 and pinned OUT. `isPaused` (watchOS 11.0) stays off as `activityRingsPaused` — it would be this package's first `@available` gate. **Authorization:** `HKObjectType.activitySummaryType()` is a THIRD kind of read type (neither quantity nor category), so it reaches the sheet through its own `activitySummaries` flag — the `sleep`/`workoutHistory` precedent — and asks for exactly ONE row, unlike `workoutHistory`: a summary is a single object HealthKit hands over whole, goals included, with no per-sample grants behind it. Apple states summaries can be read but never shared, which is already all this bridge asks for (`toShare: []`). No new entitlement and no new Info.plist KEY, but a new sheet ROW, so the plugin's default read sentence gained "your activity rings and their goals" (the 15c6840 under-promise bug, fifth occurrence), pinned in `plugin.test.ts`. **Verification level:** the day parse (a day's EXISTENCE is checked arithmetically too, not with `DateComponents.isValidDate`, whose answer runs through the calendar's time zone: Pacific/Apia has no 2011-12-30 and Kiritimati no 1994-12-31, so `isValidDate` would refuse a well-formed date on some watches and accept it on others — and refuse it with a message blaming the caller's FORMAT), the calendar invariant, the inclusive day count (Hinnant `days_from_civil` arithmetic, so no refusal can depend on the watch's time zone — including the zones where local midnight does not exist on a DST day), the ceiling and every refusal message are ① (`HealthQueryPlanTests`, `swift test` **401** on Linux); ARCH-11 fixtures both directions ① (`invoke-queryActivitySummaries-request/response.json`, the response carrying a move-TIME day with both goals `null` and a MISSING day between the two rows, so the nullable half and the gap are decoded by the strict Swift decoder rather than asserted in prose); the producer's key set ① (`invoke-producer-keys.test.ts`); the predicate/plan wiring, the live-vs-deprecated goal spellings and the no-DateComponents-in-the-bridge rule ① by textual scan (`health-package-guards.test.ts`). ② on this PR's required `watchos-tests` job: that the read type IS an `HKActivitySummaryType` and neither of the two kinds that could have ridden the `read` list, that a rings-only plan reaches the sheet as exactly one row while the quantity/sleep/workout asks never drag it in, that HealthKit's own predicate builder ACCEPTS the day components and matches on `HKPredicateKeyPathDateComponents`, and that the move-mode table maps both cases with the wire vocabulary exactly reachable. ③ device-only for the whole row, unchanged — the sim build is signed without the `healthkit` entitlement, so no simulator run can return a ring. **2026-08-20 (fifth increment) — LIVE FOREGROUND UPDATES (`startHealthUpdates`), the last of the two gaps the maintainer named and the only one in this wave that is not a one-shot read.** Every health method until now answered a question and stopped; a screen showing today's steps or the current heart rate had to poll. This SUBSCRIBES: an `HKAnchoredObjectQueryDescriptor` (watchOS 8.5, `results(for:)` -> an `AsyncSequence`, re-verified against the docs JSON) per quantity type, whose new samples are pushed as `health.samples.<type>` until the matching stop. **Two channels, deliberately:** delivery on the name-routed event channel (a stream has no single answer to resolve with), but START and STOP on `invoke` — a HealthKit start is FALLIBLE (no HealthKit, an unreadable type, an authorization round trip), and the `sensor` direct method, the only other start in the package, has no reply path at all, so a stream that never armed would be a screen stuck on "—" with nothing logged. That wart is not repeated here; `startHealthUpdates` returns `{ started, stop }` so the React effect gets a synchronous cleanup (`useEffect(() => startHealthUpdates(...).stop, [])`, no async dance whose cleanup can outrun its promise) AND the failure has somewhere to go — awaited, and logged even when it is not. **NEW samples only:** `anchor: nil` means "everything matching, then updates", so the backlog is kept out by the PREDICATE — an open-ended window from the subscribe instant, under HealthKit's default overlap rule (`endDate >= start`), which is load-bearing in one direction: step and energy samples are written AFTER the minutes they cover, so a start-date rule would drop exactly the samples a live steps screen exists to show. History stays `queryHealthSamples`'s job; a late second subscriber joins the running query and sees the NEXT sample, never a replay (`health.samples.*` is deliberately absent from `REPLAYED_EVENTS` — replaying a sample fabricates a reading that did not just occur). **Coalesced by WAITING, not dropping:** every push is a bridge crossing plus a synchronous React commit, so pushes are floored at `minIntervalMs` (default 1000, ceiling 60000 — refused past it, since the floor HOLDS a batch and a ten-minute floor is a stale screen, not a throttled one). The difference from `workout.metrics` is the point: metrics are level state, so `emitMetricsIfDue` drops and loses nothing, but a sample stream is edge-triggered and a dropped batch is data nobody can get back. **Lifecycle, each hazard named:** the query is cancelled in `tearDownGeneration()` (`health.stopAllUpdates()`, next to `sensors.stopAll()` and before `runtime?.shutdown()`) because the push path is name-routed with NO generation guard, so a surviving query delivers into the next runtime; it is FOREGROUND-ONLY by design (no background-delivery entitlement is asked for — that stays `healthBackground`), stopped on `.background` and re-armed on `.active` from a desired-state latch that survives the pause, with the JSDoc telling a caller to re-read its displayed number on the same foreground rather than pretending nothing was missed; the authorization window is guarded by a per-type EPOCH, not just a want-flag, because React StrictMode's stop-then-restart lands inside it and a want-flag would let the superseded start arm a second query whose handle is immediately overwritten — an orphan pushing duplicates with nothing left to cancel it (the same epoch is what lets a stream HealthKit ends on its own clear its handle and come back on the next foreground, the way the heart-rate pump recovers from an outside kill); and the OFF-MAIN hop is answered by isolation rather than by a hop, since the bridge is `@MainActor` and the query's `Task` inherits it (which is why this file needs none of `WorkoutBridge`'s `nonisolated(unsafe)` idiom). On the JS side the shared stream is refcounted by identity TOKENS (`startSensor`'s rule: a stale cleanup removes only its own token), the cleanup is latched against a double call, listeners key on a fresh entry so two components passing the SAME function each fire once, and a failed start clears the per-type state so the next subscriber retries instead of listening to a stream nobody armed. **Deliberately not built:** `deletedObjects` (no sample identity on the wire), sleep/rings streams (neither is a quantity sample), a force-stop-all-subscribers escape hatch, and background delivery. **Verification level:** the type vocabulary, the interval default/ceiling and every refusal message ① (`HealthQueryPlanTests`, `swift test` **407** on Linux) — including the EVENT NAME, which is the one unchecked string on both sides and now has a single definition per side (`HealthUpdatesPlan.eventPrefix`) with `health-package-guards.test.ts` pinning the JS constant against the Swift source and asserting neither the bridge nor the host spells it a second time; ARCH-11 request fixtures for both methods ①, decoded by the shipped decoders in `InvokeContractTests`; the subscribe/unsubscribe/two-subscriber/failed-start/no-replay behaviour ① (`health.test.ts`); the anchor+predicate+no-limit decisions, the coalescing-by-waiting, the teardown ORDER, the background/foreground policy, the epoch guard, the main-actor isolation and the EVENT PAYLOAD's key set (driven off the schema's own `HealthSample` fields — the gap the ARCH-11 producer scan cannot cover, since that scan reads invoke responses and this payload rides the event channel) ① by textual scan. ② on this PR's required `watchos-tests` job: that the anchored descriptor binds for all fourteen read types with no anchor and no limit, and that HealthKit's own builder makes the open-ended predicate match on the sample END date and not its start. ③ device-only for the whole row, unchanged. |
| **Workout control** (`startWorkout`/`pause`/`resume`/`endWorkout`/`getWorkoutState` + `workout.state`/`workout.metrics`) — feature `workouts` | ② | **2026-07-29.** watchOS runs ONE `HKWorkoutSession` per process, so `WorkoutSessionOwner` is now the single construction site and both the hidden heart-rate pump and the explicit API take a claim on it — `startHeartRate` is unchanged from JS's side. Request validation + the 81-case activity vocabulary ① (`WorkoutPlanTests`, `codegen.test`, generated name→case switch); ARCH-11 fixtures ①; the session lifecycle, the save and the route are ② and **③ device-only** (same entitlement reason as above). A workout does not survive a runtime reload — deliberately: teardown ends + SAVES it and parks the summary for the fresh runtime's first `getWorkoutState()`. It DOES survive process death: `recoverActiveWorkoutSession` (watchOS 5.0, the `workoutRecovery` follow-up) adopts a session left running by a launch that crashed, once per process, with the heart-rate pump deferred for the round trip so it cannot take the single slot and kill what is being recovered. A session killed from outside is reported by IDENTITY, not by a claim read — the explicit workout publishes `ended` exactly once, a killed pump publishes nothing at all (`pumpKillIsInvisible`) and its stream returns on the next foreground. State machine executed against the REAL bridges on Linux via shim modules (45 checks, red/green), which is the only in-repo proof: the owner is `#if os(watchOS)` and unreachable from `swift test` |
| **WorkoutKit plans** (`requestWorkoutPlanAuthorization`, `scheduleWorkoutPlan`, `listScheduledWorkoutPlans`, `removeScheduledWorkoutPlan`, `removeAllScheduledWorkoutPlans`, `openWorkoutPlanInWorkoutApp`) — feature `workoutPlans` | ③ sim (scheduling) / **③ device owed for `openInWorkoutApp`** | **2026-07-29.** Compose a `CustomWorkout`/`SingleGoalWorkout`/`PacerWorkout` (interval blocks, warmup/cooldown, the four goals, all nine alert types), hand it to Apple's Workout app, and schedule/list/remove it. Everything used is watchOS 10.0 — the package is `@available`-FREE, and the four symbols above the floor (`WorkoutStep.displayName` @11.0, `poolSwimDistanceWithTime` @11.0, the power `metric:` selector @10.4) are cut for exactly that reason. `WorkoutActivityType` and `WorkoutLocation` are reused verbatim, so there is no new vocabulary to drift. **The honesty mechanism is the headline:** `WorkoutScheduler.schedule`/`remove`/`removeAllWorkouts` are non-throwing and return NOTHING — they resolve identically whether the plan was stored, the user denied authorization, or the device is over quota — so every mutation is **read back** from `scheduledWorkouts` before the invoke settles, and a scheduler that stored nothing rejects `UNAVAILABLE` with text saying so. Request validation, the kind-vs-field coherence rules and the `atMs` ↔ `DateComponents` round trip (minute granularity, `Calendar` as a parameter) are in `ReactWatchSupport` → ① (`WorkoutPlanSpecTests` + `WorkoutPlanScheduleTests`, `swift test` 372); ARCH-11 fixtures both directions ①; 22 textual guards over the watchOS-only bridge ① (`workout-plan-guards.test.ts` — statelessness, read-back-after-every-mutation, the `supports*` preflight ordering, the runtime quota read, auth-reads-before-prompting). **② landed 2026-08-06** (watch-sim build + suite): the predicted loud failures were real — `countPerMinute` lives on `WorkoutAlertMetric`, not `UnitFrequency`; `WorkoutScheduler` ships un-annotated for Swift 6 (every member `get async`/nonisolated on a non-Sendable class, so the @MainActor bridge needs `@preconcurrency import WorkoutKit`); and `WorkoutPlanGoalSpec`'s memberwise init is internal to Support, so the bridge's `goal(_:)` now takes the optional and defaults `.open` — all fixed the same day. **③ landed for the uncertain half (2026-08-06 sim spike, all 7 steps answered — see design-workout-plans.md §6 RESULTS):** watch-side **scheduling WORKS on-sim** — the auth sheet appears and resolves `authorized`, `schedule`+read-back persists, 41 consecutive schedules were accepted (quota > 41 on sim; the WWDC "15" doesn't bind there), `removeAllWorkouts` read-back-verified, and all of it under the `healthkit`-less sim entitlement set (no entitlement needed — the first health-adjacent feature that sim-verifies, as hoped). The one INVERSION of the doc's guess: `openInWorkoutApp()` is what fails on the sim (`The request to open "com.apple.SessionTrackerApp" failed.` — surfaced verbatim through the rejection) — that half stays **device-③**. Also unverifiable at ANY level, stated rather than implied: **the activity × location × goal × alert legality matrix** — only Apple's binary knows it, so the code asks `supports*` and the tests pin only that it asks and that the refusal names the path. See [design-workout-plans.md](./design-workout-plans.md) |
| BLE bridge — `bleConnect`/`bleWrite`/`bleSubscribe` settle as promises (CX-022) + event push (`ble.state`/`ble.notify`) + bounded auto-reconnect | ② | invoke-result reporting is **in code** per [design-ble-result-reporting.md](./design-ble-result-reporting.md): correlation in the pure `BleSession` ① (Linux `swift test`; `.withResponse` FIFO ack via `didWriteValueFor`), bridge builds ② (last Mac build 2026-07-06; the 07-08 audit's bounded auto-reconnect — 5×60s, tunable — compiled watch-sim ② 2026-08-06). Remaining: ③ **device-verify only** — connect/drop/reconnect/write-ack/subscribe need a real peripheral (no simulator radio) |
| Complications + Smart Stack widgets (timeline, snapshot) | ③ | `WidgetSnapshot.currentIndex` ① (`WidgetSnapshotTests`); widget scheme builds green ②; **on-sim ③ (2026-07-05, Ultra 3):** the widget extension loads in `ClockFace` and renders shared App-Group data on the watch face — a Groceries complication showing "1 of 4 done" with a matching progress bar, reading the same store the app writes (`pnpm run:watch` re-signs the `.appex` with the App Group — a manual re-sign is required because the sim strips App-Group entitlements). The write→live-refresh loop (app write → WidgetKit reload → face updates) is WidgetKit-scheduled and not yet timed on-sim |
| Widget payload provenance (ARCH-06: `stateRevision` + `releaseId` on `PublishedWidgets`, monotonic App-Group revision bumped before every Storage write, `WidgetSnapshot.freshness` gate, republish reconciliation on first commit + foreground) | ① | pure core + wire ① (2026-07-27): `PayloadFreshnessTests` pins the revision→release→time precedence and the acceptance's crash-between-mutation-and-publication case, `StateRevisionTrackerTests` the batching, `StateRevisionCounterTests` the namespace isolation + monotonicity, `NewestPayloadTests` the revision ordering, `HostBridgeTests` the zero-arg trampoline through a real JSRuntime (`swift test` 272); JS stamping + lost-publication simulation (suite 434). The wiring that mints and reconciles it (`ReactWatchHost`/`WidgetIntentRuntime`/`ReactTimeline`) is watchOS-only → ② (watch-sim build+test 2026-08-06); the real two-process race and the `NSFileCoordinator` atomicity itself are device-only ③ (same standing caveat as ARCH-05's counter) |
| Complication **accented rendering** (`widgetRenderingMode`) | ② Swift / ③ **text+symbol path device-verified**, 🔴 filled-shape branch still owed | **2026-07-29.** WidgetKit, watchOS 9.0 (docs JSON, under the floor). On `.accented` Apple replaces a view's colour "while preserving the view's alpha channel", so a filled `background` becomes an opaque block in the face's colour with the same-group text invisible on it — `WidgetBackground` strokes instead of filling. Guard-tested ① (`platform-data-guards.test.ts`), both branches. No simulator renders an accented complication, so the visual outcome is device-only. **③ 2026-08-11, physical Apple Watch Ultra 3 (watchOS 26.5), reported from the adagia session:** adagia's four complication families, rendered through `ReactWatchWidget`, were placed on a real face and the face tint changed — they follow the accent colour (white → red) and stay correct in the dark / always-on state. Scope of that proof, precisely: adagia's complications are **text plus one SF Symbol** (`book.closed`) — no filled shapes, no `Gauge` — so it confirms the text/symbol path under `.accented`. The stroke-instead-of-fill branch, the one that exists because a filled `background` turns into an opaque block that hides same-group text, is still UNPROVEN on hardware: it needs a Hydration-style filled/gauge complication on a face |
| Smart Stack **relevance ranking** (per-entry score/duration) | ② | `TimelineEntryRelevance(score:duration:)` wired in `ReactWidgets.swift` (CX-017) |
| WidgetKit **Controls** — button `actionLabel` + `ControlWidgetToggle` state | ① JS / ② Swift | **2026-07-28.** `ControlDefinition`/`PublishedControl` gained `actionLabel` and `value`; `value`'s PRESENCE marks a control a toggle (`reactControlToggle` returns nil without it, so a toggle whose `isOn` nobody publishes can't render). `value` takes `boolean \| (() => boolean)` — **the getter is the correct form**: `registerControl` runs once at startup, so a literal freezes the startup state and the toggle draws itself stuck. JS ① incl. the getter contract, throwing-getter isolation, and a full round trip through the real engine in `qjs-smoke` (published false → `SetValueIntent` → React handler → republished true); suite 481. Demo `RemindersControl` + `SetRemindersIntent` are watchOS-only → **② landed 2026-08-06** (B3: the watch-sim build compiled the package targets and `run:watch` built the app targets, demo `RemindersControl` included); Control Center behavior stays ③. Configurable controls (`AppIntentControlConfiguration`) + push (`ControlPushHandler`) **declined with a trigger** (WA26-D4). `registerControl` **re-labels** a Swift-declared control and cannot create one — documented in [ui-guide.md](./ui-guide.md#complications--widgets-react-authored) (moved out of the README 2026-07-29). |
| Smart Stack **predictive `relevantContexts`** (all 8 RelevanceKit clue families) | ① wire / ② mapping | **2026-07-28 — reshaped to a tagged union** (breaking; project rule 1, no shim). `RelevantContext` now covers `date` (+ `dateKind`), `dateRange`, `location`, `poi`, `inferredLocation`, `fitness`, `sleep`, `headphones`; `reactRelevantContext(from:)` is a `switch` on `kind`. **Availability is per-arm, by gate not floor raise:** `poi`/`dateRange`/explicit `dateKind` are `@available(watchOS 26.0, *)` and return nil below; the other six are watchOS **10.0** — i.e. free at our floor and a plain backlog miss, not a watchOS-26 item. Condition cases read from Apple docs JSON: Fitness = activityRingsIncomplete\|workoutActive, Sleep = bedtime\|wakeup, Headphones = connected, Inferred = home\|work\|school\|commute (all 10.0); DateKind = default\|informational\|scheduled (26.0). **①** = the wire decode: `PublishedRelevantContextTests` decodes one clue of every kind, pins that each arm carries only its own fields, and rejects a kind-less clue (`swift test` 307) — replacing the previous fixture, which was a literal `null`. First authored usage also landed (`js/demo/widgets.tsx`); the path had none before. **Owed:** the `switch` itself is watchOS-only → **② (2026-08-06 watch-sim build+test — C6 cleared)** — and actual Smart-Stack surfacing stays **permanently ③** (device-only, C7). `ReactWatchWidget` now imports MapKit for `MKPointOfInterestCategory` (ARCH-10 box-3 note amended in the same commit). **2026-08-22 — the vocabularies are pinned cross-language and the permission story is recorded.** The name enums the union carries (the 8 `kind` discriminants, 73 POI categories, `dateKind`, inferred places, fitness/sleep/headphones conditions, the 100 m radius default) were hand-mirrored TS↔Swift with nothing pinning them — an unmapped name is dropped BY DESIGN (forward-compat), so a typo'd member would type-check, pass every JS test, and silently never surface the widget; `widget-relevance-guards.test.ts` now scans `reactRelevantContext`/`reactPoiCategory`/`reactRelevantDateKind` textually against `Record<Union, true>` tables (the `health-package-guards` idiom, since the switches are `#if os(watchOS)` and Linux can't compile them) and requires same-NAMED mapping, not same-counted. Permission findings from the per-symbol docs JSON, recorded on the JS type + [ui-guide.md](./ui-guide.md): the three location families need the APP to hold CoreLocation When-in-Use/Always authorization (Apple states it verbatim on each clue page), `fitness` needs the workout-type / exercise+move+stand-time HealthKit grants (`appleMoveTime` is not in the health read vocabulary yet — recorded gap, `requestHealthAuthorization` can ask for the other two plus the workout type), `sleep` needs `sleepAnalysis`, and an ungranted clue silently "doesn't have an effect" — plus one honest open question: whether clue evaluation also needs the widget extension's `NSWidgetWantsLocation` key (Apple's pages point to the widget-location article without saying so; device-only to answer). `date(interval:kind:)` (26.0) checked and declined — start+duration is the same signal `dateRange` spells as from/to. Providers hand the clues to WidgetKit via `relevance()` (`TimelineProvider.relevance()` and `AppIntentTimelineProvider.relevance()` are both watchOS **11.0** per docs JSON, so the `@available(watchOS 11.0, *)` gate is exactly the API floor, not caution). |
| OTA update channel — signed, anti-rollback, transactional, keyId rotation | ② | heavy logic ① (`UpdatePlan`/`OTARecord`/`VersionPolicy` + Node↔CryptoKit interop in `swift test`); host applies it ②. See [ota-signing.md](./ota-signing.md). **2026-07-28 (ARCH-08 residue, closed):** a failed OTA evaluation no longer falls back to the shipped bundle in the SAME QuickJS context — `replaceRuntimeAfterPoisonedOTA()` shuts the poisoned runtime down and boots shipped into a fresh one, so the half-executed bundle's module-scope leftovers (armed timers, widget/intent registrations, a started inspector, module singletons) cannot follow it. The sequencer properties the host now depends on (shipped eval runs last, exactly once, with the crash-loop budget already reconciled) are pinned on Linux — `swift test` 298 → 300 — but the host half is **watchOS-only: ① for the sequencer contract, ② for the swap itself — compile-verified 2026-08-06 (`ReactWatchHost` built in the watch-sim run)**, and the residue-free fallback is a ③ sim/device observation (stage a bundle that arms a timer then throws, confirm the shipped boot starts clean) |
| Liquid Glass (`glass`), double-tap (`primaryAction`), button styles (`buttonStyle="glass\|glassProminent\|plain"`) | ② **app only** | `#available(watchOS 26.0 / 11.0)`-gated for glass; GlassButtonStyle verified watchOS 26.0 via Apple docs JSON; `plain` (ungated) strips chrome for custom controls; builds ②. **2026-07-28 — `glass` and `buttonStyle` are silent NO-OPS in complications/Smart Stack widgets** (the widget's `applyLayout` mirrors `LayoutModifier` only; its interactive button hard-codes `.buttonStyle(.plain)`). Previously undocumented AND invisible to the drift gate, which scanned only `case` blocks while these reads live in the shared `body` chain / the `glassStyled` helper. Now declared in `codegen/schema.ts` `propDegradations`, documented on the props and in [capabilities.md](./api/capabilities.md), and cross-checked against `interpreter-prop-parity.golden.json` in both directions. Custom glass containers/morph APIs are **declined with a trigger** (WA26-D1): watchOS gets the reskin without an SDK rebuild, Apple calls the watch delta "minimal", and the APIs carry two rendering-performance warnings. **Owed: A3** — an Xcode-26 SDK rebuild eyeball of `List`/`Button`/`NavigationStack`/`TabView` and anything using `theme.radius`/`theme.space` (we hard-code metrics, which is the one thing Apple's guidance flags). |
| **watchOS 27 reorderable containers** (drag-to-rearrange in List/Grid) | ⛔ **Blocked** | Same SDK gate as on-device AI: symbols need Xcode 27 to compile (CX-002 precedent). Track for when the macOS workflow moves to Xcode 27. ToolbarSpacer checked and NOT on watchOS (docs JSON) — not a gap; `SpacerSizing` likewise absent. **2026-07-28 additions to re-check at Xcode 27:** (a) `CLCircularRegion` — which `reactRelevantContext` constructs — is `deprecatedAt: "27.0"` in favor of `CLCircularGeographicCondition`, but **that type has no watchOS availability at all** and `RelevantContext.location(_:)` still takes a `CLRegion`, so there is a deprecation with no successor and nothing to migrate to (WA26-D5, record-only — gating early is the CX-002 failure mode). Expect a warning, not an error. (b) `toolbarMinimizationBehavior(_:for:)` and `ToolbarItemVisibilityPriority` are watchOS 27.0 **beta**. (c) MapKit's 11 newest POI categories (`airportTerminal`, `scenicView`, `visitorCenter`, …) are watchOS 27.0 beta and are deliberately excluded from `PoiCategory`. |
| Layout modifiers (`padding`/`frame`/`background`/`cornerRadius`/`opacity`/`tint`, stack `alignment`) | ② | parsing ① (`RNStyleModifierTests`, wire pass-through in [primitives.test.tsx](../js/test/primitives.test.tsx)); the SwiftUI application (NodeView `LayoutModifier` + widget parity chain) watch-sim compile+test green (2026-07-04) — visual correctness is ③ |
| Theme/token layer (`useTheme`/`createTheme`/`ThemeProvider`) | ① | pure JS — tokens resolve before the wire ([theme.test.tsx](../js/test/theme.test.tsx)); nothing native to gate |
| Per-node `animation` prop | ② | parsing ① (`RNStyleAnimationTests`); `.animation(_:value:)` application watch-sim compile+test green (2026-07-04); actual motion is ③. Widgets ignore it by design |
| Boot-time OTA signature re-verify (NF-35) | ② | signedMessage parity ① (`OTARecordSignedMessageTests`); the evaluateOTA hook watch-sim compile+test green (2026-07-04) |
| **On-device AI** (`generateText` / Foundation Models) | ⛔ **Blocked** (gate fixed) | Gate corrected to **`watchOS 27.0`** (Apple docs: Foundation Models' `LanguageModelSession` is watchOS 27.0+ beta) and `maxTokens` wired to `GenerationOptions.maximumResponseTokens` (CX-002). The FoundationModels block still can't be **built** without the **watchOS 27 SDK (Xcode 27)** — on the current SDK it compiles out and `generateText` rejects "unavailable" — and no stable watchOS 27 device exists yet, so it's not usable by apps. A runtime `isOnDeviceAIAvailable()` query exists (resolves `false` until the FM path is built on watchOS 27). Remaining: device-verify on Xcode 27. |
| Device info (`getDeviceInfo` + accessibility + locale/is24Hour + `enableWaterLock`) | ② | invoke routing drift-tested ① (`codegen.test`); WKInterfaceDevice + WKAccessibility snapshot / Water Lock are watchOS-gated ② |
| MapKit POI search (`searchPOI` → MKLocalSearch), native live location (`showsUserLocation`/`followsUserLocation`), immersive full-screen `Map` (`onPress` tap-to-hide) | ③ | invoke routing drift-tested ① (`codegen.test`); `handleSearchPOI` + `RNMapView` native user location + tap gesture ②; **on-sim ③ (2026-07-05, Ultra 3):** the `/map-search` "Places" demo — a full-screen map with two small translucent round controls (search + recenter) pinned edge-to-edge in the bottom corners (`ignoresSafeArea` overlay) — shows MapKit's own blue user-location dot (`UserAnnotation`) and follows it natively (`MapCameraPosition.userLocation`), so a moving sim location tracks **smoothly with zero per-fix bridge traffic** (verified with `simctl location start` up Manhattan City Hall → NoHo → Greenwich Village: the blue dot stayed pinned dead-center while the map flowed beneath it — the earlier JS-streamed marker stuttered because every GPS tick round-tripped through the reconciler). Tapping the map hides/shows the chrome (`Map.onPress`, coexists with pan); a one-shot `getCurrentLocation` on focus prompts for permission + biases the search; searching "Park" drops red pins and the camera fits them (15 NYC parks framed), and the recenter button returns to following the dot (`cameraTrigger` snaps back even after a manual pan). Falls back to SF when location is unavailable. (watchOS input is modal — a search *button* reveals a field to tap; a button can't present the keyboard, only tapping a field can.) |
| Keychain secure storage (`Keychain.*`) | ② | invoke routing ①; Security-framework handler ② |
| Speech synthesis (`speak`/`stopSpeaking`) | ② | invoke routing ①; AVSpeechSynthesizer handler ② |
| Audio playback (`playAudio`/`stopAudio` + `onAudioFinished`) | ② | invoke routing ①; AVAudioPlayer + AVAudioSession `.playback` handler ② (downloads the URL, routes to Bluetooth/speaker) |
| Extended runtime session | ② | invoke routing ①; WKExtendedRuntimeSession handler ② (needs Info.plist session reason; device-only behavior ③). **2026-07-27 (ARCH-11 slice 5):** `startExtendedRuntimeSession` no longer resolves the instant `start()` returns — the invoke id is parked and settled from `extendedRuntimeSessionDidStart`/`didInvalidateWith`, so the promise means "running" and an immediate invalidation (the missing-Info.plist-reason case) **rejects `UNAVAILABLE` with the system's reason** instead of reporting success; "already active" rejects synchronously because it produces no delegate callback. The delegate settle itself is watchOS-only — watch-sim build+test ② (2026-08-06), and the behaviour is ③ |
| Lazy confirmed navigation (ARCH-09: structured `{handled, accepted, reason}` dispatch verdict, confirm-then-animate push, active-stack-only mounting) | ① | JS + engine ① (2026-07-16): navigation suite 27/27 pins the lazy contract, qjs-smoke drives the propose→confirm transaction in the real engine, vendored-engine bench measures the payoff (launch tree 152→48 nodes / 13.4→4.1 KB, perDispatch 1.32→~0.52 ms), `DispatchResult` in `swift test`. The Swift halves (returning dispatch, confirm-then-animate stack) are watchOS-only → ② (watch-sim build+test 2026-08-06); transition feel ③ |
| Structured diagnostics + operating budgets (ARCH-13: `Diagnostic` ring 50, `BudgetPolicy` node 1000 / JSON 256 KB / widget-render 500 ms, `onDiagnostic` JS API, inspector diagnostics ring) | ① | `Diagnostic`/`BudgetPolicy` in Support — `swift test` 227 (+12); JS `onDiagnostic` + `budgets.ts` in the vitest suite (410). Budgets WARN with hysteresis, never reject a commit. Host/widget write-sites are watchOS-only → ② (watch-sim build+test 2026-08-06) |
| HostPolicy feature authorization (ARCH-07: `policy:` allowlist on `ReactWatchRootView`/`ReactWatchWidgetOTA.configure`, per-feature generated install guards, typed `POLICY_DENIED`, policy check at OTA staging + validator) | ① | effective-set/authorize + staging/validator logic in `swift test` 238 (+11), incl. an install-filter proof in a real JSRuntime; JS 418 (codegen guard-coverage + invoke-map ≡ schema, `POLICY_DENIED` typing, policy-shrunk `__hostFeatures` OTA tests); `__hostFeatures` publishes the effective set so JS feature detection + the OTA pre-download gate obey policy unchanged. Host/widget wiring is watchOS-only → ② (watch-sim build+test 2026-08-06) |
| Invoke channel contract (ARCH-11: schema-declared request/result shapes + cross-language fixtures, runtime-closed `InvokeErrorCode` enum, JS caller-literal routing guard, INVALID_REQUEST on the three silent defaults) | ① | JS ① (2026-07-27, suite 459): `invoke-routing.test` pins every `invoke("name")` literal in `js/src` against the schema in both directions (the last of the three seams the 2026-06-27 deep dive named — the other two were already pinned by `codegen.test`), `invoke-contract.test` drives every payload-carrying wrapper through a recording host and writes the REAL payload/result to `Fixtures/invoke-*.json`, and `settle()` degrades an unrecognized native code to `INTERNAL` instead of casting it through. Swift ① (`swift test` 288): `InvokeContractTests` decodes all 24 fixtures with the schema-generated structs (emitted into the TEST target, not `ReactWatchCore`), requires the three opaque connectivity payloads to still be JSON objects, and re-decodes two of them with the shipped `NotificationPlan`/`UpdatePlan`. **Not proven at any level:** that a Swift handler reads the field names its method declares — the 31 handlers keep their own decoders (option (i) rejected, see the review heading). The reject-site conversions and the three INVALID_REQUEST fixes are watchOS-only → ② (watch-sim build+test 2026-08-06) |
| Background refresh (`scheduleBackgroundRefresh` + `onBackgroundRefresh`) | ② | invoke routing ①; full path wired — WKApplication schedule + `ReactWatchAppDelegate` fire→JS delivery (scaffold adds the `@WKApplicationDelegateAdaptor`) ② |
| Remote push — APNs (`registerForRemoteNotifications` → hex token + `onRemotePush`/`onRemotePushToken`/`onRemotePushRegistrationError`; own `"push"` HostPolicy feature; plugin `push: true` = `aps-environment`) | ① | JS + wire + Support ① (2026-07-17): remotePush suite (invoke routing, UNAVAILABLE/native-reason rejections, typed event unwrapping, a live runApp UI update off `__pushNativeEvent`'s Bool), `RemotePushWireTests` (hex token, userInfo sanitize) + `JSRuntime.pushNativeEventReturning` both-bridge-mode round-trip in Linux `swift test`; the host delegate flow (pending-registration correlation, `ReactWatchRemotePush`, the three `ReactWatchAppDelegate` methods) is watchOS-only → ② (watch-sim build+test 2026-08-06); real APNs delivery is ③ device — `xcrun simctl push` can exercise `didReceiveRemoteNotification` on a Mac sim |
| In-app purchase (StoreKit 2) | ② | invoke routing ①; StoreKit handlers ②; real purchases are App-Store-Connect + device ③ |
| DevTools | ② note | a **remote inspector** (`startInspector` tees `console.log` + tree snapshots over `fetch`), **not** the official React DevTools — QuickJS has no WebSocket transport |
| macOS / tvOS targets | — | not built; cross-platform core extraction is a roadmap bet, not current |

## Two corrected overclaims (the CX-027 trigger)

- **On-device AI is blocked, not shipped.** roadmap.md previously listed it as
  "shipped (watchOS 26+)". The wrong-version gate is now **fixed** (→ `watchOS
  27.0`, the actual Foundation Models floor) and `maxTokens` is wired, but it
  stays unreachable until an Xcode-27 / watchOS-27 build can compile and verify
  the FoundationModels path (CX-002). Until then, apps must not depend on
  `generateText`.
- **`relevantContexts` WAS partial — the overclaim is closed, the honest cap
  stays.** When CX-027 opened, the predictive half was decoded but not applied
  (CX-017). Both halves are now wired: ranking via per-entry
  `TimelineEntryRelevance` (②), predictive surfacing via
  `ReactTimelineProvider.relevance()` → RelevanceKit `RelevantContext`s in a
  `WidgetRelevance` (wire decode ①, mapping ② on the 2026-08-06 watch-sim
  build — row above). What must NOT be re-overclaimed: actual Smart Stack
  surfacing behaviour is **permanently ③** (device-only), and a clue without
  its permission grant (location authorization, the HealthKit reads) has no
  effect by Apple's design — see the row's 2026-08-22 note.

## How to keep this honest

When you mark something "shipped" anywhere in the docs, it must map to a row
here at level ② or ③ with an evidence link. New native features land at ②
(watchOS build green) and only move to ③ after a simulator/device run. If you
can't link evidence, it belongs in [roadmap.md](./roadmap.md), not here.
