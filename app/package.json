{
  "name": "react-watchos-demo",
  "version": "0.1.0",
  "private": true,
  "main": "index.js",
  "scripts": {
    "start": "expo start",
    "prebuild": "pnpm --filter react-watchos build && pnpm --filter react-watchos build:bytecode && expo prebuild -p ios --clean"
  },
  "dependencies": {
    "@bacons/apple-targets": "^5.0.0",
    "@expo/config-plugins": "~57.0.9",
    "expo": "~57.0.17",
    "react": "19.2.3",
    "react-native": "0.86.2",
    "react-native-watch-connectivity": "^2.0.0",
    "react-watchos": "workspace:*"
  },
  "devDependencies": {
    "@types/react": "~19.2.0",
    "typescript": "~6.0.3"
  }
}
