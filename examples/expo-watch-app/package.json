{
  "name": "expo-watch-app",
  "version": "0.1.0",
  "private": true,
  "description": "An Expo iPhone app that adds a watch feature powered by react-watchos",
  "main": "index.js",
  "scripts": {
    "build:targets": "node scripts/build-targets.mjs",
    "build:targets:unminified": "node scripts/build-targets.mjs --no-minify",
    "ota:serve": "node scripts/serve-ota.mjs",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "prebuild": "pnpm build:targets && expo prebuild -p ios --clean"
  },
  "dependencies": {
    "@bacons/apple-targets": "^5.0.0",
    "@expo/config-plugins": "~57.0.9",
    "expo": "~57.0.17",
    "react": "19.2.3",
    "react-native": "0.86.2",
    "react-native-watch-connectivity": "^2.0.0",
    "react-reconciler": "0.33.0",
    "react-watchos": "workspace:*"
  },
  "devDependencies": {
    "@babel/core": "^8.0.0",
    "@babel/preset-react": "^8.0.0",
    "@babel/preset-typescript": "^8.0.0",
    "@types/node": "^24.13.3",
    "@types/react": "~19.2.0",
    "babel-plugin-react-compiler": "^1.0.0",
    "esbuild": "^0.28.1",
    "typescript": "~6.0.3",
    "vitest": "^4.1.11"
  }
}
