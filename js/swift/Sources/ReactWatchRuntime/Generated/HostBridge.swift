// @generated by js/codegen/generate.ts — DO NOT EDIT.
// Run `npm run codegen` after editing js/codegen/schema.ts.

import CQuickJS

/// Which embedding is installing the bridge — selects which host functions
/// are exposed on `__host`. Generated from each method's schema `targets`.
public enum HostTarget: Sendable {
    case watch
    case widget
}

/// Every synchronous `__host` callback the JS bridge dispatches to. The
/// embedding host (ReactWatchHost / IntentRuntime) sets the feature closures;
/// JSRuntime sets the infra ones (setTimer/clearTimer/log) to internal
/// defaults. `via: "invoke"` methods are NOT here — they go through the
/// generic invoke channel.
public struct HostBridge {
    public var commit: ((String) -> Void)?
    public var log: ((String) -> Void)?
    public var setTimer: ((Int, Double) -> Void)?
    public var clearTimer: ((Int) -> Void)?
    public var invoke: ((Int, String, String) -> Void)?
    public var publishWidgets: ((String) -> Void)?
    public var getItem: ((String) -> String?)?
    public var setItem: ((String, String) -> Void)?
    public var counterGet: ((String) -> Int)?
    public var counterAdd: ((String, Int, Int, Int) -> Int)?
    public var stateRevision: (() -> Int)?
    public var playHaptic: ((String) -> Void)?
    public var cancelNotification: ((String) -> Void)?
    public var fetch: ((Int, String) -> Void)?
    public var abortFetch: ((Int) -> Void)?
    public var ble: ((String) -> Void)?
    public var sensor: ((String) -> Void)?
    public var generate: ((Int, String) -> Void)?
    public var cancelGenerate: ((Int) -> Void)?

    public init() {}
}

extension JSRuntime {
    /// Installs the generated host functions onto `__host` for `target`.
    /// Watch-only functions are absent in the widget so JS feature detection
    /// is correct there and a stray call fails loudly, not silently hangs.
    /// `allowedFeatures` is the HostPolicy ceiling (ARCH-07): nil installs
    /// everything the target backs; otherwise only allowed features' methods
    /// are installed — except "core" (commit/log/timers/invoke), which is
    /// always installed because the runtime can't operate without it.
    func installHostBridge(
        into host: JSValue, context: OpaquePointer, target: HostTarget,
        allowedFeatures: Set<String>?
    ) {
        func allows(_ feature: String) -> Bool {
            allowedFeatures?.contains(feature) ?? true
        }
        JS_SetPropertyStr(
            context, host, "commit",
            JS_NewCFunction(context, hostCommit, "commit", 1))
        JS_SetPropertyStr(
            context, host, "log",
            JS_NewCFunction(context, hostLog, "log", 1))
        JS_SetPropertyStr(
            context, host, "setTimer",
            JS_NewCFunction(context, hostSetTimer, "setTimer", 2))
        JS_SetPropertyStr(
            context, host, "clearTimer",
            JS_NewCFunction(context, hostClearTimer, "clearTimer", 1))
        JS_SetPropertyStr(
            context, host, "invoke",
            JS_NewCFunction(context, hostInvoke, "invoke", 3))
        JS_SetPropertyStr(
            context, host, "stateRevision",
            JS_NewCFunction(context, hostStateRevision, "stateRevision", 0))
        if allows("widgets") {
            JS_SetPropertyStr(
                context, host, "publishWidgets",
                JS_NewCFunction(context, hostPublishWidgets, "publishWidgets", 1))
        }
        if allows("storage") {
            JS_SetPropertyStr(
                context, host, "getItem",
                JS_NewCFunction(context, hostGetItem, "getItem", 1))
            JS_SetPropertyStr(
                context, host, "setItem",
                JS_NewCFunction(context, hostSetItem, "setItem", 2))
            JS_SetPropertyStr(
                context, host, "counterGet",
                JS_NewCFunction(context, hostCounterGet, "counterGet", 1))
            JS_SetPropertyStr(
                context, host, "counterAdd",
                JS_NewCFunction(context, hostCounterAdd, "counterAdd", 4))
        }
        if target == .watch {
            if allows("haptics") {
                JS_SetPropertyStr(
                    context, host, "playHaptic",
                    JS_NewCFunction(context, hostPlayHaptic, "playHaptic", 1))
            }
            if allows("notifications") {
                JS_SetPropertyStr(
                    context, host, "cancelNotification",
                    JS_NewCFunction(context, hostCancelNotification, "cancelNotification", 1))
            }
            if allows("network") {
                JS_SetPropertyStr(
                    context, host, "fetch",
                    JS_NewCFunction(context, hostFetch, "fetch", 2))
                JS_SetPropertyStr(
                    context, host, "abortFetch",
                    JS_NewCFunction(context, hostAbortFetch, "abortFetch", 1))
            }
            if allows("bluetooth") {
                JS_SetPropertyStr(
                    context, host, "ble",
                    JS_NewCFunction(context, hostBle, "ble", 1))
            }
            if allows("sensors") {
                JS_SetPropertyStr(
                    context, host, "sensor",
                    JS_NewCFunction(context, hostSensor, "sensor", 1))
            }
            if allows("ai") {
                JS_SetPropertyStr(
                    context, host, "generate",
                    JS_NewCFunction(context, hostGenerate, "generate", 2))
                JS_SetPropertyStr(
                    context, host, "cancelGenerate",
                    JS_NewCFunction(context, hostCancelGenerate, "cancelGenerate", 1))
            }
        }
    }
}

// @convention(c) trampolines: no captures, so each recovers its JSRuntime
// from the context opaque pointer, marshals the args, and dispatches.

private func hostCommit(
    ctx: OpaquePointer?, thisVal _: JSValue, argc: Int32,
    argv: UnsafeMutablePointer<JSValue>?
) -> JSValue {
    guard let runtime = JSRuntime.from(context: ctx), let argv, argc >= 1,
        let arg0Cstr = JS_ToCString(ctx, argv[0])
    else { return qjs_undefined() }
    let arg0 = String(cString: arg0Cstr)
    JS_FreeCString(ctx, arg0Cstr)
    runtime.bridge.commit?(arg0)
    return qjs_undefined()
}

private func hostLog(
    ctx: OpaquePointer?, thisVal _: JSValue, argc: Int32,
    argv: UnsafeMutablePointer<JSValue>?
) -> JSValue {
    guard let runtime = JSRuntime.from(context: ctx), let argv, argc >= 1,
        let arg0Cstr = JS_ToCString(ctx, argv[0])
    else { return qjs_undefined() }
    let arg0 = String(cString: arg0Cstr)
    JS_FreeCString(ctx, arg0Cstr)
    runtime.bridge.log?(arg0)
    return qjs_undefined()
}

private func hostSetTimer(
    ctx: OpaquePointer?, thisVal _: JSValue, argc: Int32,
    argv: UnsafeMutablePointer<JSValue>?
) -> JSValue {
    guard let runtime = JSRuntime.from(context: ctx), let argv, argc >= 2
    else { return qjs_undefined() }
    var arg0: Int32 = 0
    JS_ToInt32(ctx, &arg0, argv[0])
    var arg1: Double = 0
    JS_ToFloat64(ctx, &arg1, argv[1])
    runtime.bridge.setTimer?(Int(arg0), arg1)
    return qjs_undefined()
}

private func hostClearTimer(
    ctx: OpaquePointer?, thisVal _: JSValue, argc: Int32,
    argv: UnsafeMutablePointer<JSValue>?
) -> JSValue {
    guard let runtime = JSRuntime.from(context: ctx), let argv, argc >= 1
    else { return qjs_undefined() }
    var arg0: Int32 = 0
    JS_ToInt32(ctx, &arg0, argv[0])
    runtime.bridge.clearTimer?(Int(arg0))
    return qjs_undefined()
}

private func hostInvoke(
    ctx: OpaquePointer?, thisVal _: JSValue, argc: Int32,
    argv: UnsafeMutablePointer<JSValue>?
) -> JSValue {
    guard let runtime = JSRuntime.from(context: ctx), let argv, argc >= 3,
        let arg1Cstr = JS_ToCString(ctx, argv[1]), let arg2Cstr = JS_ToCString(ctx, argv[2])
    else { return qjs_undefined() }
    let arg1 = String(cString: arg1Cstr)
    let arg2 = String(cString: arg2Cstr)
    JS_FreeCString(ctx, arg1Cstr)
    JS_FreeCString(ctx, arg2Cstr)
    var arg0: Int32 = 0
    JS_ToInt32(ctx, &arg0, argv[0])
    runtime.bridge.invoke?(Int(arg0), arg1, arg2)
    return qjs_undefined()
}

private func hostPublishWidgets(
    ctx: OpaquePointer?, thisVal _: JSValue, argc: Int32,
    argv: UnsafeMutablePointer<JSValue>?
) -> JSValue {
    guard let runtime = JSRuntime.from(context: ctx), let argv, argc >= 1,
        let arg0Cstr = JS_ToCString(ctx, argv[0])
    else { return qjs_undefined() }
    let arg0 = String(cString: arg0Cstr)
    JS_FreeCString(ctx, arg0Cstr)
    runtime.bridge.publishWidgets?(arg0)
    return qjs_undefined()
}

private func hostGetItem(
    ctx: OpaquePointer?, thisVal _: JSValue, argc: Int32,
    argv: UnsafeMutablePointer<JSValue>?
) -> JSValue {
    guard let runtime = JSRuntime.from(context: ctx), let argv, argc >= 1,
        let arg0Cstr = JS_ToCString(ctx, argv[0])
    else { return qjs_null() }
    let arg0 = String(cString: arg0Cstr)
    JS_FreeCString(ctx, arg0Cstr)
    guard let result = runtime.bridge.getItem?(arg0) else { return qjs_null() }
    return JS_NewString(ctx, result)
}

private func hostSetItem(
    ctx: OpaquePointer?, thisVal _: JSValue, argc: Int32,
    argv: UnsafeMutablePointer<JSValue>?
) -> JSValue {
    guard let runtime = JSRuntime.from(context: ctx), let argv, argc >= 2,
        let arg0Cstr = JS_ToCString(ctx, argv[0]), let arg1Cstr = JS_ToCString(ctx, argv[1])
    else { return qjs_undefined() }
    let arg0 = String(cString: arg0Cstr)
    let arg1 = String(cString: arg1Cstr)
    JS_FreeCString(ctx, arg0Cstr)
    JS_FreeCString(ctx, arg1Cstr)
    runtime.bridge.setItem?(arg0, arg1)
    return qjs_undefined()
}

private func hostCounterGet(
    ctx: OpaquePointer?, thisVal _: JSValue, argc: Int32,
    argv: UnsafeMutablePointer<JSValue>?
) -> JSValue {
    guard let runtime = JSRuntime.from(context: ctx), let argv, argc >= 1,
        let arg0Cstr = JS_ToCString(ctx, argv[0])
    else { return JS_NewInt32(ctx, 0) }
    let arg0 = String(cString: arg0Cstr)
    JS_FreeCString(ctx, arg0Cstr)
    let result = runtime.bridge.counterGet?(arg0) ?? 0
    return JS_NewInt32(ctx, Int32(result))
}

private func hostCounterAdd(
    ctx: OpaquePointer?, thisVal _: JSValue, argc: Int32,
    argv: UnsafeMutablePointer<JSValue>?
) -> JSValue {
    guard let runtime = JSRuntime.from(context: ctx), let argv, argc >= 4,
        let arg0Cstr = JS_ToCString(ctx, argv[0])
    else { return JS_NewInt32(ctx, 0) }
    let arg0 = String(cString: arg0Cstr)
    JS_FreeCString(ctx, arg0Cstr)
    var arg1: Int32 = 0
    JS_ToInt32(ctx, &arg1, argv[1])
    var arg2: Int32 = 0
    JS_ToInt32(ctx, &arg2, argv[2])
    var arg3: Int32 = 0
    JS_ToInt32(ctx, &arg3, argv[3])
    let result = runtime.bridge.counterAdd?(arg0, Int(arg1), Int(arg2), Int(arg3)) ?? 0
    return JS_NewInt32(ctx, Int32(result))
}

private func hostStateRevision(
    ctx: OpaquePointer?, thisVal _: JSValue, argc: Int32,
    argv: UnsafeMutablePointer<JSValue>?
) -> JSValue {
    guard let runtime = JSRuntime.from(context: ctx)
    else { return JS_NewInt32(ctx, 0) }
    let result = runtime.bridge.stateRevision?() ?? 0
    return JS_NewInt32(ctx, Int32(result))
}

private func hostPlayHaptic(
    ctx: OpaquePointer?, thisVal _: JSValue, argc: Int32,
    argv: UnsafeMutablePointer<JSValue>?
) -> JSValue {
    guard let runtime = JSRuntime.from(context: ctx), let argv, argc >= 1,
        let arg0Cstr = JS_ToCString(ctx, argv[0])
    else { return qjs_undefined() }
    let arg0 = String(cString: arg0Cstr)
    JS_FreeCString(ctx, arg0Cstr)
    runtime.bridge.playHaptic?(arg0)
    return qjs_undefined()
}

private func hostCancelNotification(
    ctx: OpaquePointer?, thisVal _: JSValue, argc: Int32,
    argv: UnsafeMutablePointer<JSValue>?
) -> JSValue {
    guard let runtime = JSRuntime.from(context: ctx), let argv, argc >= 1,
        let arg0Cstr = JS_ToCString(ctx, argv[0])
    else { return qjs_undefined() }
    let arg0 = String(cString: arg0Cstr)
    JS_FreeCString(ctx, arg0Cstr)
    runtime.bridge.cancelNotification?(arg0)
    return qjs_undefined()
}

private func hostFetch(
    ctx: OpaquePointer?, thisVal _: JSValue, argc: Int32,
    argv: UnsafeMutablePointer<JSValue>?
) -> JSValue {
    guard let runtime = JSRuntime.from(context: ctx), let argv, argc >= 2,
        let arg1Cstr = JS_ToCString(ctx, argv[1])
    else { return qjs_undefined() }
    let arg1 = String(cString: arg1Cstr)
    JS_FreeCString(ctx, arg1Cstr)
    var arg0: Int32 = 0
    JS_ToInt32(ctx, &arg0, argv[0])
    runtime.bridge.fetch?(Int(arg0), arg1)
    return qjs_undefined()
}

private func hostAbortFetch(
    ctx: OpaquePointer?, thisVal _: JSValue, argc: Int32,
    argv: UnsafeMutablePointer<JSValue>?
) -> JSValue {
    guard let runtime = JSRuntime.from(context: ctx), let argv, argc >= 1
    else { return qjs_undefined() }
    var arg0: Int32 = 0
    JS_ToInt32(ctx, &arg0, argv[0])
    runtime.bridge.abortFetch?(Int(arg0))
    return qjs_undefined()
}

private func hostBle(
    ctx: OpaquePointer?, thisVal _: JSValue, argc: Int32,
    argv: UnsafeMutablePointer<JSValue>?
) -> JSValue {
    guard let runtime = JSRuntime.from(context: ctx), let argv, argc >= 1,
        let arg0Cstr = JS_ToCString(ctx, argv[0])
    else { return qjs_undefined() }
    let arg0 = String(cString: arg0Cstr)
    JS_FreeCString(ctx, arg0Cstr)
    runtime.bridge.ble?(arg0)
    return qjs_undefined()
}

private func hostSensor(
    ctx: OpaquePointer?, thisVal _: JSValue, argc: Int32,
    argv: UnsafeMutablePointer<JSValue>?
) -> JSValue {
    guard let runtime = JSRuntime.from(context: ctx), let argv, argc >= 1,
        let arg0Cstr = JS_ToCString(ctx, argv[0])
    else { return qjs_undefined() }
    let arg0 = String(cString: arg0Cstr)
    JS_FreeCString(ctx, arg0Cstr)
    runtime.bridge.sensor?(arg0)
    return qjs_undefined()
}

private func hostGenerate(
    ctx: OpaquePointer?, thisVal _: JSValue, argc: Int32,
    argv: UnsafeMutablePointer<JSValue>?
) -> JSValue {
    guard let runtime = JSRuntime.from(context: ctx), let argv, argc >= 2,
        let arg1Cstr = JS_ToCString(ctx, argv[1])
    else { return qjs_undefined() }
    let arg1 = String(cString: arg1Cstr)
    JS_FreeCString(ctx, arg1Cstr)
    var arg0: Int32 = 0
    JS_ToInt32(ctx, &arg0, argv[0])
    runtime.bridge.generate?(Int(arg0), arg1)
    return qjs_undefined()
}

private func hostCancelGenerate(
    ctx: OpaquePointer?, thisVal _: JSValue, argc: Int32,
    argv: UnsafeMutablePointer<JSValue>?
) -> JSValue {
    guard let runtime = JSRuntime.from(context: ctx), let argv, argc >= 1
    else { return qjs_undefined() }
    var arg0: Int32 = 0
    JS_ToInt32(ctx, &arg0, argv[0])
    runtime.bridge.cancelGenerate?(Int(arg0))
    return qjs_undefined()
}
